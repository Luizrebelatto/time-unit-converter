import { MeasureTimeEnum } from "./enum";

export function month30Converter(typeTo: string, valueFrom: number) {
    switch (typeTo) {
        case MeasureTimeEnum.MILISECOND:
            return 
        case MeasureTimeEnum.NANOSECOND:
            return valueFrom 
        case MeasureTimeEnum.MICROSECOND:
            return valueFrom 
        case MeasureTimeEnum.MINUTE:
            return valueFrom 
        case MeasureTimeEnum.DAY:
            return valueFrom 
        case MeasureTimeEnum.SECOND:
            return valueFrom 
        case MeasureTimeEnum.WEEK:
            return valueFrom 
        case MeasureTimeEnum.MONTH_31:
            return valueFrom 
        case MeasureTimeEnum.HOUR:
            return valueFrom 
        default:
            return valueFrom;
    }
}
